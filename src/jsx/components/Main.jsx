import React from 'react';
import WatchLinks from './WatchLinks';

export default React.createClass({
    render() {
        return (
            <section>
                <header>
                    <h1>Veilleur</h1>
                </header>
                <section>
                    <WatchLinks />
                </section>
            </section>
        );
    }
});
